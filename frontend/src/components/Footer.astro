<footer class="site-footer">
    <div class="footer__inner">
        <span class="copyright">© 2026 Ignace</span>
        <nav class="footer__nav">
            <a href="#work">Work</a>
            <a href="#about">About</a>
            <a href="#contact">Contact</a>
        </nav>
        <span class="footer__tag">[ AI-Driven Web Systems ]</span>
    </div>
</footer>

<style>
    .site-footer {
        background: var(--clr-black);
        border-top: 1px solid transparent;
        border-image: linear-gradient(to right, transparent, var(--clr-brand-600), transparent) 1;
    }

    .footer__inner {
        max-width: 1200px;
        margin: 0 auto;
        padding: 1.5rem 2rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1rem;
    }

    .copyright,
    .footer__tag {
        font-family: var(--ff-mono);
        font-size: var(--text-xs);
        color: var(--clr-neutral-500);
    }

    .footer__tag {
        color: var(--clr-brand-400);
    }

    .footer__nav {
        display: flex;
        gap: 1.5rem;
    }

    .footer__nav a {
        font-family: var(--ff-mono);
        font-size: var(--text-xs);
        color: var(--clr-neutral-500);
        text-decoration: none;
        transition: color 0.2s;
    }

    .footer__nav a:hover {
        color: var(--clr-brand-400);
    }

    @media (width <= 760px) {
        .footer__inner {
            flex-direction: column;
            text-align: center;
            gap: 0.75rem;
        }
    }
</style>
