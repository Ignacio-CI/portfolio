---
import type { Project } from '../types/project';

type Props = Project;

const { title, subtitle, content, image, link, tags, status, company, companyLink }: Props = Astro.props;

const svgs = import.meta.glob<{ default: any }>('/src/assets/*.svg', { eager: true });

const filename = image.split('/').pop();

const LogoComponent = filename ? svgs[`/src/assets/${filename}`]?.default : null;
---

<div class="card">
    <div class="card__header">
        <h2>{title}</h2>
        {company && <a href={companyLink} target="_blank">{company}</a>}
        <div class="card__header-subtitle">
            <p>{subtitle}</p>
            <span class:list={[
                "status-badge",
                { prod: status === 'production' }, 
                { mvp: status === 'mvp' }, 
                { dev: status === 'development' }, 
            ]}>{status}</span>
        </div>
    </div>
    <p class="content">{content}</p>
    <div class="logo">
        {LogoComponent && <LogoComponent />}
    </div>
    <div class="tags">
        {tags.map((tag: string) => (
            <span class="tag">{tag}</span>
        ))}
    </div>
    <a href={link} target="_blank">View Project_</a>
</div>

<style>
    .card {
        width: 400px;
        height: auto;
        border: 1px solid var(--ui-border);
        padding: 1rem;
        margin: 1rem;
        border-radius: 8px;
        background-color: var(--off-white);
        color: var(--deep-slate);
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        align-items: start; 
        gap: 1rem;
    }

    .card h2 {
        font-size: var(--font-size-2xl);
        font-weight: 600;
        margin: 0;
    }

    .card a {
        width: fit-content;
    }

    .card__header {
        display: flex;
        flex-direction: column;
        justify-content: start;
        gap: 1rem;
    }

    .card__header-subtitle {
        display: flex;
        flex-direction: row;
        justify-content: start;
        align-items: center;
        gap: 1rem;
    }

    .status-badge {
        font-family: var(--font-mono);
        font-size: var(--font-size-xs);
        border: 1px solid var(--ui-border);
        padding: 0.25rem 0.5rem;
        border-radius: 10px;
        color: var(--deep-slate);
    }

    .prod {
        background-color: oklch(92.5% 0.084 155.995);
        border: 1px solid oklch(72.3% 0.219 149.579);
    }

    .mvp {
        background-color: oklch(96.2% 0.059 95.617);
        border: 1px solid oklch(76.9% 0.188 70.08);
    }

    .dev {
        background-color: oklch(95.1% 0.026 236.824);
        border: 1px solid oklch(68.5% 0.169 237.323);
    }
    
    .content {
        height: 100%;
        font-family: var(--font-mono);
        font-size: var(--font-size-sm);
        margin: 0;
    }

    .card img {
        width: 100%;
        height: auto;
        border-radius: 8px;
    }

    .card a {
        font-family: var(--font-mono);
        font-size: var(--font-size-xs);
        text-transform: uppercase;
        letter-spacing: 0.2em;
        text-decoration: none;
        color: var(--deep-slate);
    }

    .card a:hover {
        color: var(--electric-indigo);
    }

    .logo {
        display: none;
        width: 100%;
        height: auto;
        border-radius: 8px;
    }

    .tags {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
    }

    .tag {
        font-size: var(--font-size-xs);
        font-family: var(--font-sans);
        color: var(--deep-slate);
        background-color: var(--off-white);
        border: 1px solid var(--ui-border);
        border-radius: 4px;
        padding: 0.25rem 0.5rem;
    }
</style>